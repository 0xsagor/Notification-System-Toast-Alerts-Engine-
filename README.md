# Notification System (Toast Alerts)

Reusable toast notification system built with pure JavaScript.

## Features
- Success, error, and info alerts
- Auto-dismiss notifications
- Clean and reusable logic
- No dependencies

## Usage
Open `index.html` in the browser and click the buttons.

## Purpose
UI feedback and component-based design showcase for portfolio projects.
